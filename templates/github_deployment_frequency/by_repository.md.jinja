---
title: Releases per repository, per month
last_reviewed_on: {{ now }}
review_in: 3 months
---
# <%= current_page.data.title %>

Releases per month for each repository.

*Published on {{ now }} in {{ duration }}*

<table class='dataframe'>
    <thead>
        <tr>
            <th align='left' colspan='2'>Repository</th>
            {% for month in months -%}
            <th align='right'>{{ month  }}</th>
            {% endfor -%}
        </tr>
    </thead>
    <tbody>
    {% for repo, item in by_repo.items() -%}
        <tr class="totals">
            <th align='left' rowspan="2" valign="top">
                <a href='https://github.com/{{ repo }}'>{{ repo | replace("ministryofjustice/", "") }}</a>
            </th>
            <th title='Total'>T</th>
            {% for m in months -%}
            <td align='right' title="Total for {{ m }}">{{ item.get(m).get('total_success', 0)  }}</td>
            {% endfor -%}
        </tr>
        <tr class="averages">
            <td title='Average per weekday'>A</td>
            {% for m in months -%}
            <td align='right' title="Average per weekday for {{ m }}">{{ item.get(m).get('average_success', 0) | round(2) }}</td>
            {% endfor -%}
        </tr>
    {% endfor -%}
        <tr>
            <th align='left' colspan='2'>TOTALS</th>
            {% for m in months -%}
            <td align='right' title="Total for {{ m }}" >{{ totals.get(m).get('total_success', 0)  }}</td>
            {% endfor -%}
        </tr>
        <tr>
            <th align='left' colspan='2'>AVERAGE PER DAY</th>
            {% for m in months -%}
            <td align='right' title="Average per day for {{ m }}">{{ totals.get(m).get('average_success', 0) | round(2) }}</td>
            {% endfor -%}
        </tr>
    </tbody>
</table>


*Created by [opg-dora-metrics](https://github.com/ministryofjustice/opg-dora-metrics)*
