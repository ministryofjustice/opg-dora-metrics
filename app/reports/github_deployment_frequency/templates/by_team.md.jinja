---
title: Releases per team, per month
last_reviewed_on: {{ now }}
review_in: 3 months
---
# <%= current_page.data.title %>

Releases per month for each github team.

*Published on {{ now }} in {{ duration }}*

<table class='dataframe'>
    <thead>
        <tr>
            <th style='text-align:left;' rowspan='2'></th>
            {% for month in months -%}
            <th style='text-align:right;padding:10px;' colspan='2'>{{ month  }}</th>
            {% endfor -%}
        </tr>
        <tr>
            {% for month in months -%}
            <th title="Total for {{ month }}" style='text-align:right;padding:10px;'>T</th>
            <th title="Average per weekday for {{ month }}" style='text-align:right;padding:10px;'>A</th>
            {% endfor -%}
        </tr>
    </thead>
    <tbody>
    {% for team, item in by_team.items() -%}
        <tr>
            <th style='text-align:left;'>{{ team }}</th>
            {% for m in months -%}
            {% set total, avger = item.get(m) -%}
            <td title="Total for {{ m }}" style='text-align:right;padding:10px;'>{{ total  }}</td>
            <td title="Average per weekday for {{ m }}" style='text-align:right;padding:10px;'>{{ "%0.1f" | format(avger) }}</td>
            {% endfor -%}
        </tr>
    {% endfor -%}
    </tr>
    </tbody>
</table>


*Created by [opg-dora-metrics](https://github.com/ministryofjustice/opg-dora-metrics)*
